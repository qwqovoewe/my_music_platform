<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wrx.mapper.UserMapper">
    <insert id="insertUser">
        insert into user values (null,#{username},#{password},#{nickname},#{sex},#{hobby})
    </insert>
    <insert id="insertList">insert into lists values (null,#{user_id},null,null,"我喜欢的音乐")</insert>
    <update id="upateUser">
update user set nickname = #{nickname},sex=#{sex},hobby=#{hobby} where user_id =#{user_id}
    </update>
    <select id="login" resultType="com.wrx.domain.User">
        select * from user where username = #{username} and password = #{password}
    </select>
    <select id="registerJudge" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM user WHERE username = #{username};
    </select>
    <select id="editor" resultType="com.wrx.domain.User">select * from user where username = #{username} and password = #{password}</select>
    <select id="show" resultType="com.wrx.domain.User"> select * from user where user_id =  #{user_id} </select>
    <select id="getUserID" resultType="java.lang.Integer">select  user_id from user where username = #{username}</select>
<!--    <select id="showFromname" resultType="com.wrx.domain.User">select * from user where username = #{username}</select>-->

</mapper>